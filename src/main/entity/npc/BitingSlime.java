package main.entity.npc;

import main.GameController;
import main.entity.Entity;
import utilities.Position;

public class BitingSlime extends Entity
{

    public BitingSlime(GameController gc, Position position) {
        super(gc);
    }
}
